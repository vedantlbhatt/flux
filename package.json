{
  "name": "flux",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "search": "bun run src/cli.ts",
    "api": ".venv/bin/uvicorn main:app --reload --host 0.0.0.0",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix"
  },
  "devDependencies": {
    "@types/node": "^25.3.2",
    "oxlint": "^1.50.0"
  }
}
